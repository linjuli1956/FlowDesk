/*
FlowDesk 主样式表 - Windows 10/11
基于卡片式彩色按钮设计，实现UI截图的视觉效果

UI四大铁律实现：
🚫 禁止样式重复 - 统一的基类样式 + objectName定制
🔄 严格自适应布局 - 支持布局管理器的响应式设计
📏 最小宽度保护 - 设置合理的最小尺寸
⚙️ 智能组件缩放 - 灵活的尺寸策略

颜色方案：基于UI截图分析的语义化颜色系统
*/

/* ===== 全局样式 ===== */
* {
    font-family: "Microsoft YaHei UI", "Segoe UI", Arial, sans-serif;
    font-size: 9pt;
}

/* ===== ValidationErrorDialog 样式 ===== */
/* 主对话框容器 */
QDialog#validation_error_dialog {
    background: qlineargradient(x1:0, y1:0, x2:1, y2:1, 
                stop:0 #f8f9fa, stop:1 #e9ecef);
    border: 1px solid rgba(255, 255, 255, 0.3);
    border-radius: 16px;
}

/* 错误图标样式 */
QLabel#error_icon {
    font-size: 24px;
    color: #e74c3c;
    background: transparent;
    border: none;
    padding: 8px;
}

/* 错误标题样式 */
QLabel#error_title {
    font-size: 16px;
    font-weight: bold;
    color: #2c3e50;
    background: transparent;
    border: none;
    padding: 4px 8px;
}

/* 错误描述文本区域 */
QTextEdit#error_description {
    background: rgba(255, 255, 255, 0.6);
    border: 1px solid rgba(52, 152, 219, 0.2);
    border-radius: 12px;
    padding: 12px;
    font-size: 13px;
    color: #2c3e50;
    selection-background-color: rgba(52, 152, 219, 0.3);
}

/* 格式示例文本区域 */
QTextEdit#format_examples {
    background: rgba(46, 204, 113, 0.1);
    border: 1px solid rgba(46, 204, 113, 0.3);
    border-radius: 12px;
    padding: 12px;
    font-size: 13px;
    color: #27ae60;
    selection-background-color: rgba(46, 204, 113, 0.2);
}

/* 关闭按钮样式 - 符合Claymorphism设计风格 */
QDialog#validation_error_dialog QPushButton#close_error_dialog_btn {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1,
                stop:0 #2ecc71, stop:1 #27ae60);
    border: 2px solid #22995440;
    border-radius: 8px;
    color: white;
    font-weight: bold;
    min-width: 80px;
    min-height: 32px;
    padding: 8px 16px;
    font-size: 13px;
}

QDialog#validation_error_dialog QPushButton#close_error_dialog_btn:hover {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1,
                stop:0 #58d68d, stop:1 #2ecc71);
    border-color: #145a32cc;
}

QDialog#validation_error_dialog QPushButton#close_error_dialog_btn:pressed {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1,
                stop:0 #229954, stop:1 #1e8449);
    border-color: #0d3d1f;
}

/* 倒计时标签样式 */
QLabel#close_timer_label {
    color: #7f8c8d;
    font-size: 12px;
    background: transparent;
    border: none;
    padding: 4px;
}

/* Claymorphism 全局效果 */
QWidget {
    background-color: transparent;
}

/* ===== 主窗口样式 - Claymorphism ===== */
#main_window {
    background: qlineargradient(x1:0, y1:0, x2:1, y2:1, 
                stop:0 #f0f2f5, stop:0.5 #e8ebf0, stop:1 #dde1e7);
    color: --text-primary;
}

#central_widget {
    background: transparent;
    border-radius: 16px;
}

/* ===== Tab控件样式 - Claymorphism ===== */
#main_tab_widget {
    background: transparent;
    border: none;
}

#main_tab_widget::pane {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(255,255,255,0.9), stop:1 rgba(245,247,250,0.9));
    border: 1px solid rgba(255,255,255,0.3);
    border-radius: 16px;
    margin-top: -1px;
    /* Claymorphism 边框效果 - 使用Qt支持的边框和渐变属性 */
    border: 2px solid rgba(255,255,255,0.4);
    /* 使用渐变模拟内阴影效果 */
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(255,255,255,0.95), stop:0.1 rgba(245,247,250,0.9), 
                stop:0.9 rgba(245,247,250,0.9), stop:1 rgba(235,240,245,0.85));
}

#main_tab_widget::tab-bar {
    alignment: center;
}

/* Tab标签样式 - Claymorphism */
#main_tab_widget QTabBar::tab {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(255,255,255,0.6), stop:1 rgba(240,242,245,0.6));
    color: --text-primary;
    border: 1px solid rgba(255,255,255,0.4);
    border-bottom: none;
    padding: 10px 24px;
    margin-right: 3px;
    border-top-left-radius: 12px;
    border-top-right-radius: 12px;
    min-width: 90px;
    font-weight: 500;
    /* 内凹效果 - 使用渐变和边框替代阴影 */
    border: 1px solid rgba(200,205,210,0.3);
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(235,240,245,0.8), stop:1 rgba(245,250,255,0.9));
}

#main_tab_widget QTabBar::tab:selected {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(255,255,255,0.95), stop:1 rgba(248,250,252,0.95));
    border-bottom: none;
    color: --text-primary;
    /* 突出效果 - 使用边框和渐变 */
    border: 2px solid rgba(255,255,255,0.6);
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(255,255,255,0.95), stop:1 rgba(250,252,255,0.9));
}

#main_tab_widget QTabBar::tab:hover:!selected {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(255,255,255,0.8), stop:1 rgba(245,247,250,0.8));
    /* 悬停效果 - 使用边框变化 */
    border: 1px solid rgba(255,255,255,0.5);
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(250,252,255,0.9), stop:1 rgba(245,248,252,0.85));
}

/* ===== Tab页面内容样式 ===== */
#network_config_tab, #network_tools_tab, #rdp_tab, #hardware_tab {
    background-color: --background-secondary;
    border-radius: 8px;
}

/* Tab页面占位符标签 */
#network_config_tab_placeholder,
#network_tools_tab_placeholder,
#rdp_tab_placeholder,
#hardware_tab_placeholder {
    color: --text-secondary;
    font-size: 12pt;
    background-color: transparent;
    border: 2px dashed --border-color;
    border-radius: 8px;
    padding: 40px;
    margin: 20px;
}

/* ===== Claymorphism 按钮基础样式 ===== */
QPushButton {
    border: 1px solid rgba(255,255,255,0.3);
    border-radius: 12px;
    padding: 12px 20px;
    font-weight: 500;
    font-size: 9pt;
    min-width: 80px;
    min-height: 32px;
    /* Claymorphism 效果 - 使用边框和渐变替代阴影 */
    border: 2px solid rgba(255,255,255,0.4);
    /* 使用多层渐变模拟立体效果 */
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(255,255,255,0.9), stop:0.5 rgba(248,250,252,0.85), stop:1 rgba(240,245,250,0.8));
}

QPushButton:hover {
    /* 悬停时增强效果 - 使用更亮的渐变 */
    border: 2px solid rgba(255,255,255,0.6);
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(255,255,255,0.95), stop:0.5 rgba(252,254,255,0.9), stop:1 rgba(245,250,255,0.85));
}

QPushButton:pressed {
    /* 按下时内凹效果 - 使用反向渐变 */
    border: 2px solid rgba(200,205,210,0.4);
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(230,235,240,0.8), stop:1 rgba(245,250,255,0.9));
}

QPushButton:disabled {
    opacity: 0.4;
    /* 禁用状态 - 使用淡化的边框和渐变 */
    border: 1px solid rgba(220,225,230,0.3);
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(240,245,250,0.6), stop:1 rgba(250,252,255,0.7));
}

/* ===== Claymorphism 彩色按钮样式 - 柔和渐变色 ===== */

/* 退出对话框按钮样式 - 直接使用objectName选择器 */

/* 取消按钮 - 蓝色渐变（主要操作） */
#tray_exit_cancel_btn {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 #F0F7FF, stop:1 #E1EFFF);
    color: #4A7BA7;
    border: 1px solid #D1E7FF;
}

#tray_exit_cancel_btn:hover {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 #E1EFFF, stop:1 #D1E7FF);
}

#tray_exit_cancel_btn:pressed {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 #D1E7FF, stop:1 #C1DFFF);
}

/* 最小化按钮 - 绿色渐变（成功操作） */
#tray_exit_no_btn {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 #F0F9F0, stop:1 #E1F5E1);
    color: #4A7C59;
    border: 1px solid #D1F1D1;
}

#tray_exit_no_btn:hover {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 #E1F5E1, stop:1 #D1F1D1);
}

#tray_exit_no_btn:pressed {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 #D1F1D1, stop:1 #C1EDC1);
}

/* 退出按钮 - 红色渐变（危险操作） */
#tray_exit_yes_btn {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 #FFF5F5, stop:1 #FFE9E9);
    color: #B85450;
    border: 1px solid #FFDDDD;
}

#tray_exit_yes_btn:hover {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 #FFE9E9, stop:1 #FFDDDD);
}

#tray_exit_yes_btn:pressed {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 #FFDDDD, stop:1 #FFD1D1);
}

/* 通用彩色按钮样式 - 用于其他地方的按钮 */
QPushButton[class="btn-blue"] {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 #F0F7FF, stop:1 #E1EFFF);
    color: #4A7BA7;
    border: 1px solid #D1E7FF;
}

QPushButton[class="btn-blue"]:hover {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 #E1EFFF, stop:1 #D1E7FF);
}

QPushButton[class="btn-blue"]:pressed {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 #D1E7FF, stop:1 #C1DFFF);
}

QPushButton[class="btn-green"] {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 #F0F9F0, stop:1 #E1F5E1);
    color: #4A7C59;
    border: 1px solid #D1F1D1;
}

QPushButton[class="btn-green"]:hover {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 #E1F5E1, stop:1 #D1F1D1);
}

QPushButton[class="btn-green"]:pressed {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 #D1F1D1, stop:1 #C1EDC1);
}

QPushButton[class="btn-red"] {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 #FFF5F5, stop:1 #FFE9E9);
    color: #B85450;
    border: 1px solid #FFDDDD;
}

QPushButton[class="btn-red"]:hover {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 #FFE9E9, stop:1 #FFDDDD);
}

QPushButton[class="btn-red"]:pressed {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, stop:0 #FFDDDD, stop:1 #FFD1D1);
}

/* 橙色按钮 - 警告/工具操作（静态IP、DHCP） */
QPushButton[class="btn-orange"] {
    background: --btn-orange;
    color: --btn-orange-text;
    border: 1px solid --btn-orange-border;
}

QPushButton[class="btn-orange"]:hover {
    background: --btn-orange-hover;
}

QPushButton[class="btn-orange"]:pressed {
    background: --btn-orange-pressed;
}

/* 紫色按钮 - 特殊功能（Win11自动登录、计算机名） */
QPushButton[class="btn-purple"] {
    background: --btn-purple;
    color: --btn-purple-text;
    border: 1px solid --btn-purple-border;
}

QPushButton[class="btn-purple"]:hover {
    background: --btn-purple-hover;
}

QPushButton[class="btn-purple"]:pressed {
    background: --btn-purple-pressed;
}

/* 青色按钮 - 网络相关工具 */
QPushButton[class="btn-cyan"] {
    background: --btn-cyan;
    color: --btn-cyan-text;
    border: 1px solid --btn-cyan-border;
}

QPushButton[class="btn-cyan"]:hover {
    background: --btn-cyan-hover;
}

QPushButton[class="btn-cyan"]:pressed {
    background: --btn-cyan-pressed;
}

/* ===== Claymorphism 输入框样式 ===== */
QLineEdit, QTextEdit, QPlainTextEdit {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(255,255,255,0.8), stop:1 rgba(248,250,252,0.8));
    border: 1px solid rgba(255,255,255,0.4);
    border-radius: 10px;
    padding: 8px 12px;
    color: --text-primary;
    selection-background-color: rgba(116, 181, 249, 0.3);
    /* 内凹效果 - 使用内边框和渐变 */
    border: 2px solid rgba(220,225,230,0.4);
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(240,245,250,0.9), stop:1 rgba(250,255,255,0.95));
}

QLineEdit:focus, QTextEdit:focus, QPlainTextEdit:focus {
    border: 2px solid rgba(116, 181, 249, 0.6);
    /* 焦点效果 - 使用彩色边框和渐变 */
    border: 2px solid rgba(116, 181, 249, 0.6);
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(245,250,255,0.95), stop:1 rgba(240,248,255,0.9));
}

QLineEdit::placeholder, QTextEdit::placeholder, QPlainTextEdit::placeholder {
    color: --input-placeholder;
}

/* ===== Claymorphism 下拉框样式 ===== */
QComboBox {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(255,255,255,0.8), stop:1 rgba(248,250,252,0.8));
    border: 1px solid rgba(255,255,255,0.4);
    border-radius: 10px;
    padding: 8px 12px;
    color: --text-primary;
    min-width: 120px;
    /* 内凹效果 - 使用边框和渐变 */
    border: 2px solid rgba(220,225,230,0.4);
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(240,245,250,0.9), stop:1 rgba(250,255,255,0.95));
}

QComboBox:focus {
    border: 2px solid rgba(116, 181, 249, 0.6);
    /* 焦点效果 - 使用彩色边框 */
    border: 2px solid rgba(116, 181, 249, 0.6);
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(245,250,255,0.95), stop:1 rgba(240,248,255,0.9));
}

QComboBox::drop-down {
    border: none;
    width: 20px;
}

QComboBox::down-arrow {
    image: url(data:image/svg+xml;base64,);
}

QComboBox QAbstractItemView {
    background-color: --input-background;
    border: 1px solid --border-color;
    border-radius: 4px;
    selection-background-color: --btn-blue;
    selection-color: --text-inverse;
}

/* ===== 标签样式 ===== */
QLabel {
    color: --text-primary;
    background-color: transparent;
}

QLabel[class="title"] {
    font-size: 12pt;
    font-weight: bold;
    color: --text-primary;
}

QLabel[class="subtitle"] {
    font-size: 10pt;
    color: --text-secondary;
}

/* ===== 状态徽章样式 ===== */
QLabel[class="badge"] {
    border-radius: 12px;
    padding: 4px 8px;
    font-size: 8pt;
    font-weight: bold;
    color: --text-inverse;
}

QLabel[class="badge-connected"] {
    background-color: --badge-connected;
}

QLabel[class="badge-disconnected"] {
    background-color: --badge-disconnected;
}

QLabel[class="badge-dhcp"] {
    background-color: --badge-dhcp;
}

QLabel[class="badge-static"] {
    background-color: --badge-static;
}

/* ===== Claymorphism 卡片容器样式 ===== */
QWidget[class="card"] {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(255,255,255,0.9), stop:1 rgba(245,247,250,0.9));
    border: 1px solid rgba(255,255,255,0.3);
    border-radius: 16px;
    margin: 6px;
    /* Claymorphism 卡片效果 - 使用边框和渐变 */
    border: 2px solid rgba(255,255,255,0.5);
    background: qlineargradient(x1:0, y1:0, x2:1, y2:1, 
                stop:0 rgba(255,255,255,0.9), stop:0.5 rgba(250,252,255,0.85), stop:1 rgba(245,248,252,0.8));
}

QGroupBox {
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(255,255,255,0.9), stop:1 rgba(245,247,250,0.9));
    border: 1px solid rgba(255,255,255,0.3);
    border-radius: 14px;
    margin-top: 12px;
    padding-top: 12px;
    font-weight: 500;
    /* 卡片效果 - 使用边框和渐变 */
    border: 1px solid rgba(255,255,255,0.4);
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(255,255,255,0.85), stop:1 rgba(248,252,255,0.8));
}

QGroupBox::title {
    subcontrol-origin: margin;
    left: 10px;
    padding: 0 8px 0 8px;
    color: --text-primary;
}

/* ===== 系统托盘对话框样式 ===== */
#tray_exit_dialog {
    background-color: --dialog-background;
    border: 1px solid --dialog-border;
    border-radius: 8px;
}

#tray_exit_message {
    color: --text-primary;
    font-size: 11pt;
    background-color: transparent;
}

/* ===== 菜单样式 ===== */
QMenu {
    background-color: --menu-background;
    border: 1px solid --border-color;
    border-radius: 4px;
    padding: 4px;
}

QMenu::item {
    background-color: transparent;
    padding: 6px 20px;
    border-radius: 3px;
    color: --text-primary;
}

QMenu::item:selected {
    background-color: --menu-hover;
}

QMenu::separator {
    height: 1px;
    background-color: --menu-separator;
    margin: 4px 8px;
}

/* ===== 滚动条样式 ===== */
QScrollBar:vertical {
    background-color: --background-primary;
    width: 12px;
    border-radius: 6px;
}

QScrollBar::handle:vertical {
    background-color: --border-color;
    border-radius: 6px;
    min-height: 20px;
}

QScrollBar::handle:vertical:hover {
    background-color: --text-secondary;
}

QScrollBar::add-line:vertical, QScrollBar::sub-line:vertical {
    height: 0px;
}

QScrollBar:horizontal {
    background-color: --background-primary;
    height: 12px;
    border-radius: 6px;
}

QScrollBar::handle:horizontal {
    background-color: --border-color;
    border-radius: 6px;
    min-width: 20px;
}

QScrollBar::handle:horizontal:hover {
    background-color: --text-secondary;
}

QScrollBar::add-line:horizontal, QScrollBar::sub-line:horizontal {
    width: 0px;
}

/* ===== 表格样式 ===== */
QTableWidget, QTableView {
    background-color: --background-secondary;
    alternate-background-color: --background-tertiary;
    gridline-color: --border-color;
    border: 1px solid --border-color;
    border-radius: 4px;
}

QTableWidget::item, QTableView::item {
    padding: 8px;
    border: none;
}

QTableWidget::item:selected, QTableView::item:selected {
    background-color: --btn-blue;
    color: --text-inverse;
}

QHeaderView::section {
    background-color: --tab-inactive;
    color: --text-primary;
    padding: 8px;
    border: 1px solid --border-color;
    font-weight: bold;
}

/* ===== 进度条样式 ===== */
QProgressBar {
    background-color: --background-tertiary;
    border: 1px solid --border-color;
    border-radius: 4px;
    text-align: center;
    color: --text-primary;
}

QProgressBar::chunk {
    background-color: --btn-blue;
    border-radius: 3px;
}

/* ===== 复选框和单选框样式 ===== */
QCheckBox, QRadioButton {
    color: --text-primary;
    spacing: 8px;
}

QCheckBox::indicator, QRadioButton::indicator {
    width: 16px;
    height: 16px;
}

QCheckBox::indicator:unchecked {
    border: 2px solid --border-color;
    background-color: --input-background;
    border-radius: 3px;
}

QCheckBox::indicator:checked {
    border: 2px solid --btn-blue;
    background-color: --btn-blue;
    border-radius: 3px;
}

QRadioButton::indicator:unchecked {
    border: 2px solid --border-color;
    background-color: --input-background;
    border-radius: 8px;
}

QRadioButton::indicator:checked {
    border: 2px solid --btn-blue;
    background-color: --btn-blue;
    border-radius: 8px;
}

/* ===== 工具提示样式 ===== */
QToolTip {
    background-color: --text-primary;
    color: --text-inverse;
    border: 1px solid --border-color;
    border-radius: 4px;
    padding: 4px 8px;
    font-size: 9pt;
}

/* ===== QMessageBox 消息框样式 - Claymorphism 设计 ===== */
/*
作用说明：
为系统消息框提供统一的Claymorphism视觉风格，确保与主界面保持一致性。
采用面向对象设计原则，通过CSS选择器实现样式与功能的分离，
支持成功、错误、信息等不同类型消息框的主题化显示。

面向对象架构特点：
- 封装性：将消息框样式封装在主样式文件中统一管理
- 单一职责：专门负责QMessageBox组件的视觉呈现
- 开闭原则：可通过objectName扩展不同主题而不修改基础样式
- 依赖倒置：样式定义与具体消息内容解耦
*/

/* QMessageBox 基础容器样式 */
QMessageBox {
    /* Claymorphism 背景：柔和的毛玻璃卡片效果 */
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 rgba(255,255,255,0.95), stop:1 rgba(245,247,250,0.95));
    
    /* 卡片式圆角设计，符合现代UI趋势 */
    border-radius: 12px;
    
    /* Claymorphism 边框：半透明白色边框营造浮起效果 */
    border: 2px solid rgba(255,255,255,0.6);
    
    /* 文字颜色：深灰色确保良好的可读性 */
    color: #2c3e50;
    
    /* 字体设置：使用系统默认字体确保兼容性 */
    font-family: "Microsoft YaHei UI", "Segoe UI", Arial, sans-serif;
    font-size: 10pt;
    
    /* 最小尺寸：确保消息框有足够的显示空间 */
    min-width: 300px;
    min-height: 120px;
}

/* QMessageBox 文本标签样式 */
QMessageBox QLabel {
    /* 消息文本样式：清晰的深色文字 */
    color: #1f2937;
    font-size: 10pt;
    
    /* 文字边距：提供适当的间距 */
    margin: 8px 12px;
    
    /* 背景透明：继承父容器背景 */
    background: transparent;
    border: none;
}

/* QMessageBox 确定按钮样式 - 蓝色渐变 */
QMessageBox QPushButton[text="确定"] {
    /* 确定按钮：蓝色渐变背景突出重要性 */
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 #3b82f6, stop:1 #2563eb);
    
    /* 蓝色边框：与背景色协调 */
    border: 2px solid #1d4ed8;
    border-radius: 6px;
    
    /* 按钮尺寸：确保良好的点击体验 */
    min-width: 70px;
    min-height: 28px;
    padding: 6px 14px;
    
    /* 白色文字：在蓝色背景上提供良好对比度 */
    color: white;
    font-weight: 600;
    font-size: 9pt;
}

/* 确定按钮悬停状态 */
QMessageBox QPushButton[text="确定"]:hover {
    /* 悬停效果：更深的蓝色渐变 */
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 #2563eb, stop:1 #1d4ed8);
    border: 2px solid #1e40af;
}

/* 确定按钮按下状态 */
QMessageBox QPushButton[text="确定"]:pressed {
    /* 按下效果：最深的蓝色渐变 */
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 #1d4ed8, stop:1 #1e40af);
    border: 2px solid #1e3a8a;
}

/* QMessageBox 取消按钮样式 - 灰色渐变 */
QMessageBox QPushButton[text="取消"] {
    /* 取消按钮：灰色渐变背景表示次要操作 */
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 #6b7280, stop:1 #4b5563);
    
    /* 灰色边框：与背景色协调 */
    border: 2px solid #374151;
    border-radius: 6px;
    
    /* 按钮尺寸：与确定按钮保持一致 */
    min-width: 70px;
    min-height: 28px;
    padding: 6px 14px;
    
    /* 白色文字：在灰色背景上确保可读性 */
    color: white;
    font-weight: 500;
    font-size: 9pt;
}

/* 取消按钮悬停状态 */
QMessageBox QPushButton[text="取消"]:hover {
    /* 悬停效果：更深的灰色渐变 */
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 #4b5563, stop:1 #374151);
    border: 2px solid #1f2937;
}

/* 取消按钮按下状态 */
QMessageBox QPushButton[text="取消"]:pressed {
    /* 按下效果：最深的灰色渐变 */
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 #374151, stop:1 #1f2937);
    border: 2px solid #111827;
}

/* QMessageBox 通用按钮样式 - 适用于其他按钮 */
QMessageBox QPushButton {
    /* 通用按钮：浅蓝色渐变背景 */
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 #e0f2fe, stop:1 #b3e5fc);
    
    /* 浅蓝色边框：柔和的视觉效果 */
    border: 2px solid #81d4fa;
    border-radius: 6px;
    
    /* 按钮尺寸：确保良好的点击体验 */
    min-width: 70px;
    min-height: 28px;
    padding: 6px 14px;
    
    /* 深色文字：在浅色背景上确保对比度 */
    color: #0277bd;
    font-weight: 500;
    font-size: 9pt;
}

/* 通用按钮悬停状态 */
QMessageBox QPushButton:hover {
    /* 悬停效果：更深的浅蓝色渐变 */
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 #b3e5fc, stop:1 #81d4fa);
    border: 2px solid #4fc3f7;
    color: #01579b;
}

/* 通用按钮按下状态 */
QMessageBox QPushButton:pressed {
    /* 按下效果：最深的浅蓝色渐变 */
    background: qlineargradient(x1:0, y1:0, x2:0, y2:1, 
                stop:0 #81d4fa, stop:1 #4fc3f7);
    border: 2px solid #29b6f6;
    color: #01579b;
}

/* ===== 主样式文件结束 ===== */
/* 所有Tab专用样式已移至各自的QSS文件中管理，遵循StylesheetService模块化设计原则 */
